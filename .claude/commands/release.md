---
description: Cut a new MemoDismiss release (calls the release skill)
---

Invoke the `release` skill to cut a new MemoDismiss release.

Follow the skill's procedure exactly: precondition checks, version bump proposal, tag message draft (both gated by user approval), tag creation and push, GitHub Actions run monitoring, and final release verification.

$ARGUMENTS
